# Agent Operations

Meta-skills: skills about running agents well. This is where heavy agent users get disproportionate returns.

Expected: 7 skills.

Skills are generated one at a time from the build prompts under `meta-prompts/agent-operations/`. Each lands in its own folder here as `agent-operations/<skill-name>/SKILL.md`.
