# Core Infrastructure

The primitives other skills call. Build these first, because several skills and most runbooks in this library depend on them.

Expected: 5 skills.

Skills are generated one at a time from the build prompts under `meta-prompts/core-infrastructure/`. Each lands in its own folder here as `core-infrastructure/<skill-name>/SKILL.md`.
